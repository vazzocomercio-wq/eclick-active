import { Kanban } from 'lucide-react';
import { PlaceholderPage } from '@/components/placeholder-page';

export default function FunisPage() {
  return (
    <PlaceholderPage
      title="Funis"
      subtitle="Kanban de oportunidades por etapa"
      icon={Kanban}
    />
  );
}
