import { MessageSquare } from 'lucide-react';
import { PlaceholderPage } from '@/components/placeholder-page';

export default function ConversasPage() {
  return (
    <PlaceholderPage
      title="Conversas"
      subtitle="Inbox unificado de todos os canais"
      icon={MessageSquare}
    />
  );
}
