import { Bot } from 'lucide-react';
import { PlaceholderPage } from '@/components/placeholder-page';

export default function CopilotoPage() {
  return (
    <PlaceholderPage
      title="Copiloto IA"
      subtitle="Assistente de IA pra acelerar seu trabalho"
      icon={Bot}
    />
  );
}
